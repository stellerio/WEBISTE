import * as THREE from 'three'; export function createAtmosphere(){const g=new THREE.BufferGeometry();const n=1400;const a=new Float32Array(n*3);for(let i=0;i<a.length;i++)a[i]=(Math.random()-.5)*34;g.setAttribute('position',new THREE.BufferAttribute(a,3));return new THREE.Points(g,new THREE.PointsMaterial({color:'#bfeaff',size:.018,transparent:true,opacity:.45,depthWrite:false,blending:THREE.AdditiveBlending}));}